# Secrets for the IMX462 RPi Controller. Copy to `.env` (git-ignored) and fill in.

# MQTT broker
MQTT_HOST=broker.example.com
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# OpenTelemetry (optional token/auth)
OTEL_EXPORTER_OTLP_HEADERS=

# CA certificate (PEM) path for the OTLP endpoint when it uses a private CA.
# Blank = system CAs. The cert ships as a git-ignored file via the app role.
OTEL_EXPORTER_OTLP_CERTIFICATE=

# Stable per-host resource identity: merged into every OTel resource so
# metrics/traces/logs from multiple Pis can be split per host
# (Prometheus `instance` label, Loki `host_name` label).
# Use this Pi's hostname.
OTEL_RESOURCE_ATTRIBUTES=host.name=my-pi,service.instance.id=my-pi
